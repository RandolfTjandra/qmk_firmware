run

```
qmk compile -kb boardsource/unicorne -km randolf
```

Then you have to flash both sides of the board separately.

Each side you hold the outer top most key while plugging it in to
enter it into boot mode.

The keyboard should be recognized as a drive. You just have to drag
and drop the firmware into there.
