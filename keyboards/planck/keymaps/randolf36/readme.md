# Randolf36

## cmd

make planck/rev6_drop:randolf36

Changes so far

-   Put lower on F
-   Put backspace on left “space” so I can get rid of the top right one
-   Removed lower on F, moved layers to backspace and space
-   I put shift on Z and on A on hold layers
-   I put / on the raise layer where m is
-   Remove the original backspace
-   Put return where RAISE used to be
-   Remove original return
-   Hold a for ctl and tap for a
-   Put tab where lower used to be
-   Adjust the behavior of the mod tapping so I can type at normal speed

<!-- function foobar(param: string) { -->
<!-- console.log(param); -->
<!-- } -->
